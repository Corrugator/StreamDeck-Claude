# Dependencies
node_modules/

# Runtime logs (contain local usernames/paths)
logs/
com.corrugator.streamclaude.sdPlugin/logs/

# Build artifacts (bundle + compiled keychain helper — rebuilt via `npm run build`)
com.corrugator.streamclaude.sdPlugin/bin/
*.streamDeckPlugin
.build/

# Release staging (assembled from marketplace/ + pack output)
deployment/

# Local tooling & OS noise
.claude/settings.local.json
.DS_Store
